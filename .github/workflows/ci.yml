name: CI

on:
  push:
    branches: ['**']
    paths-ignore:
      - '**/*.md'
  pull_request:
    branches: ['**']
    paths-ignore:
      - '**/*.md'
  workflow_call:

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  lint:
    name: Lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Install ShellCheck
        run: sudo apt-get update && sudo apt-get install -y shellcheck
      - name: ShellCheck
        run: shellcheck scripts/*.sh

  test:
    name: Test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Create fixture (Dockerfile in subdirectory)
        run: |
          mkdir -p testdata/subdir
          touch testdata/subdir/Dockerfile
      - name: Run script at repo root (finds subdir)
        id: resolve
        env:
          INPUT_DOCKERFILE: Dockerfile
          INPUT_WORKSPACE: ${{ github.workspace }}
          INPUT_SET_ENV: 'true'
          INPUT_VERBOSE: 'true'
          GITHUB_WORKSPACE: ${{ github.workspace }}
          GITHUB_OUTPUT: ${{ github.workspace }}/out.txt
          GITHUB_ENV: ${{ github.workspace }}/env.txt
        run: bash scripts/resolve-workflow-dir.sh
      - name: Verify outputs
        run: |
          out="${{ github.workspace }}/out.txt"
          if [ ! -f "$out" ]; then echo "::error::out.txt not found"; ls -la "${{ github.workspace }}"; exit 1; fi
          echo "Contents of out.txt:"
          cat "$out"
          # Require the three action outputs (path format varies by runner)
          grep -q 'workflow-dir=' "$out" || (echo "::error::missing workflow-dir"; exit 1)
          grep -q 'docker-build-context=' "$out" || (echo "::error::missing docker-build-context"; exit 1)
          grep -q 'docker-file=' "$out" || (echo "::error::missing docker-file"; exit 1)
          grep -q 'testdata' "$out" || (echo "::error::output should reference testdata"; exit 1)
